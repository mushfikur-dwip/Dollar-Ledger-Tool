{
  "buildCommand": "pnpm --filter @workspace/mobile run build",
  "outputDirectory": "artifacts/mobile/dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
